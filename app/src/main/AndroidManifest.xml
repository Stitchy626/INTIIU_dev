<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rokuniroku.newinti.intiiu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EventAnnPage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EventAnnItemPage" />
        <activity android:name=".EventAnnRequestPage" />
        <activity android:name=".EventAnnRequestStatusPage" />
        <activity
            android:name=".INTIAnnPage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".INTIAnnItemPage" />

    </application>

</manifest>